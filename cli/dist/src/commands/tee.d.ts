/**
 * priven tee - TEE verification commands
 *
 * Commands:
 *   priven tee verify - Verify TEE integrity
 */
import { Command } from "commander";
export declare function registerTeeCommand(program: Command): void;
